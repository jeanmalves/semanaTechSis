@model IEnumerable<SemanaAcademica.Areas.Admin.Models.ListarPalestraModel>

@{
    ViewBag.Title = "Admin - Palestras";
}

<h2>Admin - Palestras</h2>

<p>
    @Html.ActionLink("Cadastrar", "Create")
</p>
@if (Model.Count() > 0)
{
    <table>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nome)
                </td>
                <td>
                    @Html.ActionLink("Editar", "Edit", new { id = item.IdPalestra }) |
            @Html.ActionLink("Excluir", "Delete", new { id = item.IdPalestra }) |
                    @Html.ActionLink("Horario(s)", "Index", "Horario", new { id = item.IdPalestra }, null)
                </td>
            </tr>
        }

    </table>
}
